import 'package:flutter/material.dart';

class MyLobbyAway extends StatelessWidget {
  const MyLobbyAway({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
    );
  }
}